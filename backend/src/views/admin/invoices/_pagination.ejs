<% if (totalPages > 1) { %>
    <div class="pagination mt-8 flex justify-center">
        <% if (currentPage > 1) { %>
            <a href="/admin/invoices?page=<%= currentPage - 1 %><%= typeof query !== 'undefined' ? '&' + new URLSearchParams(query).toString() : '' %>"
                class="px-4 py-2 border rounded-lg mx-1 hover:bg-gray-100 transition text-gray-700">Previous</a>
        <% } %>

        <% for (let i = 1; i <= totalPages; i++) { %>
            <a href="/admin/invoices?page=<%= i %><%= typeof query !== 'undefined' ? '&' + new URLSearchParams(query).toString() : '' %>"
                class="px-4 py-2 border rounded-lg mx-1 <%= currentPage === i ? 'bg-indigo-600 text-white border-indigo-600' : 'text-gray-700 hover:bg-gray-100' %> transition">
                <%= i %>
            </a>
        <% } %>

        <% if (currentPage < totalPages) { %>
            <a href="/admin/invoices?page=<%= currentPage + 1 %><%= typeof query !== 'undefined' ? '&' + new URLSearchParams(query).toString() : '' %>"
                class="px-4 py-2 border rounded-lg mx-1 hover:bg-gray-100 transition text-gray-700">Next</a>
        <% } %>
    </div>
<% } %>
