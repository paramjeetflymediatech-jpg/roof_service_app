<header class="top-header">
  <h1><%= title %></h1>
  <div class="user-info">
    <span
      >Welcome, <%= (typeof userName !== 'undefined' ? userName : (typeof user
      !== 'undefined' && user.name ? user.name : 'Admin')) %></span
    >
  </div>
</header>
